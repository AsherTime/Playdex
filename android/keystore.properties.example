# Replace with your Play signing values. Do NOT commit real passwords.
# Copy to android/keystore.properties locally, then configure signing in app/build.gradle.

storeFile=../gamedex-release.jks
storePassword=REPLACE_ME
keyAlias=gamedex
keyPassword=REPLACE_ME
